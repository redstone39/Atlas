# atlas-pypdf built-in descriptor

Official built-in base-parser scaffold. The runtime supplies the authorized
artifact broker; this example intentionally does not open filesystem paths.
Its production implementation may use the Apache-2.0 licensed `pypdf` package.
